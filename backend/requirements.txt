# Версии закреплены точно, а не «не ниже».
#
# Раньше здесь стояло `>=`, и каждая сборка на Vercel тянула свежайшее с
# PyPI. Значит в облаке собиралось не то, что проверено локально: код тот
# же, библиотеки разные. Вычислить такое по симптому нельзя — приложение
# просто перестаёт подниматься, а наружу идёт FUNCTION_INVOCATION_FAILED
# без причины.
#
# Набор ниже — тот, на котором проходят все проверки локально. Обновлять
# осознанно: поднять версию, прогнать e2e_qa.py и e2e_corp.py, потом
# выкатывать. Само оно обновляться не должно.

fastapi==0.141.1
starlette==1.3.1          # тянется фастапи, но закрепляем явно: ставится и сам
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.14.2
sqlalchemy==2.0.51
httpx==0.28.1

# Загрузка и сжатие фотографий из админки
python-multipart==0.0.32
pillow==12.3.0

# Базы: SQLite для локальной разработки, PostgreSQL для облака
aiosqlite==0.22.1
asyncpg==0.31.0

# S3-совместимое хранилище фотографий (Supabase Storage, Cloudflare R2, S3).
# Нужно там, где нет постоянного диска.
boto3==1.43.64
