"""
Точка входа для площадок, которые сами запускают приложение.

Vercel и большинство PaaS ищут переменную `app` в файле `main.py`
или `index.py` в корне сервиса. Настоящий код живёт в пакете `app/`,
здесь только пере-экспорт, чтобы не плодить копию.

Локально по-прежнему работает и привычное:
    uvicorn app.main:app --reload

────────────────────────────────────────────────────────────────────────
ИМПОРТ ДОЛЖЕН ОСТАВАТЬСЯ НА ВЕРХНЕМ УРОВНЕ

Не оборачивать его ни в `try`, ни в функцию. Сборщик Vercel ищет имя `app`
СТАТИЧЕСКИ, не запуская файл, и на обёрнутом импорте сборка падает целиком:

    Build Failed
    Could not find a top-level "app", "application", or "handler" in "main.py"

Это уже стоило десяти дней простоя. Ради диагностики сбоя запуска импорт
здесь был завёрнут в try/except с заглушкой — и с того дня НИ ОДНА сборка
не собиралась, а прод жил на откате. Диагностика, из-за которой нечего
диагностировать, хуже её отсутствия.

Если снова понадобится ловить сбой запуска — ловить его в `app/main.py`,
внутри lifespan: там это уже сделано, и причина видна в `/health` полем
`startup_error`. Здесь — только простой импорт.
"""

from __future__ import annotations

from app.main import app

__all__ = ["app"]
